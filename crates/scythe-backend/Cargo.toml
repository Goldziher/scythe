[package]
name = "scythe-backend"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
ahash = { version = "0.8", features = ["serde"] }
minijinja = { version = "2", features = ["loader"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "1.1"
