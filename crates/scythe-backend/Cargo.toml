[package]
name = "scythe-backend"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
minijinja = { version = "2", features = ["loader"] }
thiserror = "2"
ahash = { version = "0.8", features = ["serde"] }
