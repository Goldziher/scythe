# Engine test-parity exemptions.
#
# Read by tools/integration-test-generator/tests/engine_test_parity.rs, which
# runs under `cargo test --workspace`.
#
# GH #196/#195: java.java.jinja and kotlin.kt.jinja each have one whole
# duplicated test program per SQL engine branch. An assertion added to one
# branch (almost always postgresql, the richest branch) reaches none of the
# others, and the branches drift apart silently -- the Redshift branches in
# particular ended up with fewer than half the postgresql branch's test
# functions. This file is the parity gate's ratcheting allowlist, modeled on
# coverage-exemptions.txt: it ratchets in BOTH directions and there is no
# tolerance mode.
#
#   * a (template, engine) branch missing a test function the postgresql
#     branch of the same template has, with no entry here, fails the build
#     (regression -- the drift got worse);
#   * an entry here for a test function the branch now DOES have also fails
#     the build (stale -- the gap was closed, delete the line).
#
# Format: <template>|<engine>|<test-function-name> : <reason>
#
# Every entry below is structural: "no equivalent query exists in <engine>'s
# queries/*.sql fixture" -- the postgresql-only test calls a generated
# Queries method (a UserStatus enum parameter, or a nullable composite-column
# read for board #197) that has no equivalent query defined for this engine
# at all. Closing this requires designing a per-engine equivalent query, not
# copying an assertion.
#
# The other reason shape this file used to carry -- "the underlying query
# already exists in this engine's queries fixture but the harness branch was
# never wired up to call it" -- named a closeable coverage gap rather than a
# structural incompatibility. All entries of that shape have been closed by
# porting the missing test into each branch (GH #196/#195 follow-up); none
# remain here as of this writing. A future PASS gap of that shape would look
# identical to a structural one syntactically -- check the query fixture
# before assuming it belongs in this file at all.
#
# Blank lines and lines starting with `#` are ignored.

java|duckdb|testCountUsersByStatus : no equivalent query exists in duckdb's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|duckdb|testGetUserOrders : no equivalent query exists in duckdb's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|duckdb|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; duckdb has no equivalent column or query
java|mariadb|testCountUsersByStatus : no equivalent query exists in mariadb's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|mariadb|testGetUserOrders : no equivalent query exists in mariadb's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|mariadb|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; mariadb has no equivalent column or query
java|mssql|testCountUsersByStatus : no equivalent query exists in mssql's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|mssql|testGetUserOrders : no equivalent query exists in mssql's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|mssql|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; mssql has no equivalent column or query
java|mysql|testCountUsersByStatus : no equivalent query exists in mysql's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|mysql|testGetUserOrders : no equivalent query exists in mysql's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|mysql|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; mysql has no equivalent column or query
java|oracle|testCountUsersByStatus : no equivalent query exists in oracle's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|oracle|testGetUserOrders : no equivalent query exists in oracle's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|oracle|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; oracle has no equivalent column or query
java|redshift|testCountUsersByStatus : no equivalent query exists in redshift's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|redshift|testGetUserOrders : no equivalent query exists in redshift's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|redshift|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; redshift has no equivalent column or query
java|snowflake|testCountUsersByStatus : no equivalent query exists in snowflake's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|snowflake|testGetUserOrders : no equivalent query exists in snowflake's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|snowflake|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; snowflake has no equivalent column or query
java|sqlite|testCountUsersByStatus : no equivalent query exists in sqlite's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|sqlite|testGetUserOrders : no equivalent query exists in sqlite's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
java|sqlite|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; sqlite has no equivalent column or query
kotlin|duckdb|testCountUsersByStatus : no equivalent query exists in duckdb's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|duckdb|testGetUserOrders : no equivalent query exists in duckdb's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|duckdb|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; duckdb has no equivalent column or query
kotlin|mariadb|testCountUsersByStatus : no equivalent query exists in mariadb's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|mariadb|testGetUserOrders : no equivalent query exists in mariadb's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|mariadb|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; mariadb has no equivalent column or query
kotlin|mssql|testCountUsersByStatus : no equivalent query exists in mssql's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|mssql|testGetUserOrders : no equivalent query exists in mssql's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|mssql|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; mssql has no equivalent column or query
kotlin|mysql|testCountUsersByStatus : no equivalent query exists in mysql's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|mysql|testGetUserOrders : no equivalent query exists in mysql's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|mysql|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; mysql has no equivalent column or query
kotlin|oracle|testCountUsersByStatus : no equivalent query exists in oracle's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|oracle|testGetUserOrders : no equivalent query exists in oracle's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|oracle|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; oracle has no equivalent column or query
kotlin|redshift|testCountUsersByStatus : no equivalent query exists in redshift's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|redshift|testGetUserOrders : no equivalent query exists in redshift's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|redshift|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; redshift has no equivalent column or query
kotlin|snowflake|testCountUsersByStatus : no equivalent query exists in snowflake's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|snowflake|testGetUserOrders : no equivalent query exists in snowflake's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|snowflake|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; snowflake has no equivalent column or query
kotlin|sqlite|testCountUsersByStatus : no equivalent query exists in sqlite's queries/*.sql fixture -- CountUsersByStatus takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|sqlite|testGetUserOrders : no equivalent query exists in sqlite's queries/*.sql fixture -- GetUserOrders takes a UserStatus enum parameter that only pg/queries/users.sql defines
kotlin|sqlite|testGetUserProfileNullable : exercises a nullable composite-column read (board #197) unique to the postgresql schema; sqlite has no equivalent column or query
