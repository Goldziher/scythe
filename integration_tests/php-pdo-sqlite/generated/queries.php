<?php

declare(strict_types=1);

// Auto-generated by scythe. Do not edit.


function createOrder(PDO $pdo, int $user_id, float $total, string $notes): void {
    $stmt = $pdo->prepare("INSERT INTO orders (user_id, total, notes) VALUES (?, ?, ?)");
    $stmt->execute([$user_id, $total, $notes]);
}

readonly class GetOrdersByUserRow {
    public function __construct(
        public int $id,
        public float $total,
        public ?string $notes,
        public string $created_at,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            total: (float) $row['total'],
            notes: $row['notes'] !== null ? (string) $row['notes'] : null,
            created_at: (string) $row['created_at'],
        );
    }
}

function getOrdersByUser(PDO $pdo, int $user_id): array {
    $stmt = $pdo->prepare("SELECT id, total, notes, created_at FROM orders WHERE user_id = ? ORDER BY created_at DESC");
    $stmt->execute([$user_id]);
    $rows = $stmt->fetchAll(PDO::FETCH_ASSOC);
    return array_map([GetOrdersByUserRow::class, 'fromRow'], $rows);
}

readonly class GetOrderTotalRow {
    public function __construct(
        public ?float $total_sum,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            total_sum: $row['total_sum'] !== null ? (string) $row['total_sum'] : null,
        );
    }
}

function getOrderTotal(PDO $pdo, int $user_id): ?GetOrderTotalRow {
    $stmt = $pdo->prepare("SELECT SUM(total) AS total_sum FROM orders WHERE user_id = ?");
    $stmt->execute([$user_id]);
    $row = $stmt->fetch(PDO::FETCH_ASSOC);
    return $row ? GetOrderTotalRow::fromRow($row) : null;
}

function deleteOrdersByUser(PDO $pdo, int $user_id): int {
    $stmt = $pdo->prepare("DELETE FROM orders WHERE user_id = ?");
    $stmt->execute([$user_id]);
    return $stmt->rowCount();
}

readonly class GetUserByIdRow {
    public function __construct(
        public int $id,
        public string $name,
        public ?string $email,
        public string $status,
        public string $created_at,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            name: (string) $row['name'],
            email: $row['email'] !== null ? (string) $row['email'] : null,
            status: (string) $row['status'],
            created_at: (string) $row['created_at'],
        );
    }
}

function getUserById(PDO $pdo, int $id): ?GetUserByIdRow {
    $stmt = $pdo->prepare("SELECT id, name, email, status, created_at FROM users WHERE id = ?");
    $stmt->execute([$id]);
    $row = $stmt->fetch(PDO::FETCH_ASSOC);
    return $row ? GetUserByIdRow::fromRow($row) : null;
}

readonly class ListActiveUsersRow {
    public function __construct(
        public int $id,
        public string $name,
        public ?string $email,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            name: (string) $row['name'],
            email: $row['email'] !== null ? (string) $row['email'] : null,
        );
    }
}

function listActiveUsers(PDO $pdo, string $status): array {
    $stmt = $pdo->prepare("SELECT id, name, email FROM users WHERE status = ?");
    $stmt->execute([$status]);
    $rows = $stmt->fetchAll(PDO::FETCH_ASSOC);
    return array_map([ListActiveUsersRow::class, 'fromRow'], $rows);
}

function createUser(PDO $pdo, string $name, string $email, string $status): void {
    $stmt = $pdo->prepare("INSERT INTO users (name, email, status) VALUES (?, ?, ?)");
    $stmt->execute([$name, $email, $status]);
}

function updateUserEmail(PDO $pdo, string $email, int $id): void {
    $stmt = $pdo->prepare("UPDATE users SET email = ? WHERE id = ?");
    $stmt->execute([$email, $id]);
}

function deleteUser(PDO $pdo, int $id): void {
    $stmt = $pdo->prepare("DELETE FROM users WHERE id = ?");
    $stmt->execute([$id]);
}

readonly class SearchUsersRow {
    public function __construct(
        public int $id,
        public string $name,
        public ?string $email,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            name: (string) $row['name'],
            email: $row['email'] !== null ? (string) $row['email'] : null,
        );
    }
}

function searchUsers(PDO $pdo, string $name): array {
    $stmt = $pdo->prepare("SELECT id, name, email FROM users WHERE name LIKE ?");
    $stmt->execute([$name]);
    $rows = $stmt->fetchAll(PDO::FETCH_ASSOC);
    return array_map([SearchUsersRow::class, 'fromRow'], $rows);
}
