// Auto-generated by scythe. Do not edit.
#nullable enable

using System.Data;
using Snowflake.Data.Client;

public static class Queries {

public static async Task CreateOrder(SnowflakeDbConnection conn, int user_id, long total, string? notes) {
    await using var cmd = new SnowflakeDbCommand(conn);
    cmd.CommandText = "INSERT INTO orders (user_id, total, notes) VALUES (?, ?, ?)";
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p1", Value = user_id });
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p2", Value = total });
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p3", Value = notes });
    await cmd.ExecuteNonQueryAsync();
}

public record GetOrdersByUserRow(
    int Id,
    long Total,
    string? Notes,
    DateTime CreatedAt
);

public static async Task<List<GetOrdersByUserRow>> GetOrdersByUser(SnowflakeDbConnection conn, int user_id) {
    await using var cmd = new SnowflakeDbCommand(conn);
    cmd.CommandText = "SELECT id, total, notes, created_at FROM orders WHERE user_id = ? ORDER BY created_at DESC";
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p1", Value = user_id });
    await using var reader = await cmd.ExecuteReaderAsync();
    var results = new List<GetOrdersByUserRow>();
    while (await reader.ReadAsync()) {
        results.Add(new GetOrdersByUserRow(
            reader.GetInt32(0),
            reader.GetInt64(1),
            reader.IsDBNull(2) ? null : reader.GetString(2),
            reader.GetDateTime(3)
        ));
    }
    return results;
}

public record GetOrderTotalRow(
    long? TotalSum
);

public static async Task<GetOrderTotalRow?> GetOrderTotal(SnowflakeDbConnection conn, int user_id) {
    await using var cmd = new SnowflakeDbCommand(conn);
    cmd.CommandText = "SELECT SUM(total) AS total_sum FROM orders WHERE user_id = ?";
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p1", Value = user_id });
    await using var reader = await cmd.ExecuteReaderAsync();
    if (!await reader.ReadAsync()) return null;
    return new GetOrderTotalRow(
        reader.IsDBNull(0) ? null : reader.GetInt64(0)
    );
}

public static async Task<int> DeleteOrdersByUser(SnowflakeDbConnection conn, int user_id) {
    await using var cmd = new SnowflakeDbCommand(conn);
    cmd.CommandText = "DELETE FROM orders WHERE id IN (SELECT id FROM orders WHERE user_id = ?)";
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p1", Value = user_id });
    return await cmd.ExecuteNonQueryAsync();
}

public record GetUserByIdRow(
    int Id,
    string Name,
    string? Email,
    bool Active,
    string? Metadata,
    DateTime CreatedAt,
    DateTimeOffset? UpdatedAt
);

public static async Task<GetUserByIdRow?> GetUserById(SnowflakeDbConnection conn, int id) {
    await using var cmd = new SnowflakeDbCommand(conn);
    cmd.CommandText = "SELECT id, name, email, active, metadata, created_at, updated_at FROM users WHERE id = ?";
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p1", Value = id });
    await using var reader = await cmd.ExecuteReaderAsync();
    if (!await reader.ReadAsync()) return null;
    return new GetUserByIdRow(
        reader.GetInt32(0),
        reader.GetString(1),
        reader.IsDBNull(2) ? null : reader.GetString(2),
        reader.GetBoolean(3),
        reader.IsDBNull(4) ? null : reader.GetString(4),
        reader.GetDateTime(5),
        reader.IsDBNull(6) ? null : reader.GetFieldValue<DateTimeOffset>(6)
    );
}

public record ListActiveUsersRow(
    int Id,
    string Name,
    string? Email
);

public static async Task<List<ListActiveUsersRow>> ListActiveUsers(SnowflakeDbConnection conn) {
    await using var cmd = new SnowflakeDbCommand(conn);
    cmd.CommandText = "SELECT id, name, email FROM users WHERE active = TRUE";
    await using var reader = await cmd.ExecuteReaderAsync();
    var results = new List<ListActiveUsersRow>();
    while (await reader.ReadAsync()) {
        results.Add(new ListActiveUsersRow(
            reader.GetInt32(0),
            reader.GetString(1),
            reader.IsDBNull(2) ? null : reader.GetString(2)
        ));
    }
    return results;
}

public static async Task CreateUser(SnowflakeDbConnection conn, string name, string? email, bool active) {
    await using var cmd = new SnowflakeDbCommand(conn);
    cmd.CommandText = "INSERT INTO users (name, email, active, metadata) VALUES (?, ?, ?, PARSE_JSON(?))";
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p1", Value = name });
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p2", Value = email });
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p3", Value = active });
    await cmd.ExecuteNonQueryAsync();
}

public static async Task UpdateUserEmail(SnowflakeDbConnection conn, string email, int id) {
    await using var cmd = new SnowflakeDbCommand(conn);
    cmd.CommandText = "UPDATE users SET email = ?, updated_at = CURRENT_TIMESTAMP() WHERE id = ?";
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p1", Value = email });
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p2", Value = id });
    await cmd.ExecuteNonQueryAsync();
}

public static async Task DeleteUser(SnowflakeDbConnection conn, int id) {
    await using var cmd = new SnowflakeDbCommand(conn);
    cmd.CommandText = "DELETE FROM users WHERE id = ?";
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p1", Value = id });
    await cmd.ExecuteNonQueryAsync();
}

public record SearchUsersRow(
    int Id,
    string Name,
    string? Email
);

public static async Task<List<SearchUsersRow>> SearchUsers(SnowflakeDbConnection conn, string name) {
    await using var cmd = new SnowflakeDbCommand(conn);
    cmd.CommandText = "SELECT id, name, email FROM users WHERE name LIKE ?";
    cmd.Parameters.Add(new SnowflakeDbParameter { ParameterName = "p1", Value = name });
    await using var reader = await cmd.ExecuteReaderAsync();
    var results = new List<SearchUsersRow>();
    while (await reader.ReadAsync()) {
        results.Add(new SearchUsersRow(
            reader.GetInt32(0),
            reader.GetString(1),
            reader.IsDBNull(2) ? null : reader.GetString(2)
        ));
    }
    return results;
}

}
