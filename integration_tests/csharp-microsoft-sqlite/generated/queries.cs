// Auto-generated by scythe. Do not edit.
using System.Threading.Tasks;
using Microsoft.Data.Sqlite;

public static class Queries {

public static async Task CreateOrder(SqliteConnection conn, int user_id, float total, string notes) {
    await using var cmd = new SqliteCommand("INSERT INTO orders (user_id, total, notes) VALUES (?, ?, ?)", conn);
    cmd.Parameters.AddWithValue("$p1", user_id);
    cmd.Parameters.AddWithValue("$p2", total);
    cmd.Parameters.AddWithValue("$p3", notes);
    await cmd.ExecuteNonQueryAsync();
}

public record GetOrdersByUserRow(
    int Id,
    float Total,
    string? Notes,
    string CreatedAt
);

public static async Task<List<GetOrdersByUserRow>> GetOrdersByUser(SqliteConnection conn, int user_id) {
    await using var cmd = new SqliteCommand("SELECT id, total, notes, created_at FROM orders WHERE user_id = ? ORDER BY created_at DESC", conn);
    cmd.Parameters.AddWithValue("$p1", user_id);
    await using var reader = await cmd.ExecuteReaderAsync();
    var results = new List<GetOrdersByUserRow>();
    while (await reader.ReadAsync()) {
        results.Add(new GetOrdersByUserRow(
            reader.GetInt32(0),
            reader.GetFloat(1),
            reader.IsDBNull(2) ? null : reader.GetString(2),
            reader.GetString(3)
        ));
    }
    return results;
}

public record GetOrderTotalRow(
    double? TotalSum
);

public static async Task<GetOrderTotalRow?> GetOrderTotal(SqliteConnection conn, int user_id) {
    await using var cmd = new SqliteCommand("SELECT SUM(total) AS total_sum FROM orders WHERE user_id = ?", conn);
    cmd.Parameters.AddWithValue("$p1", user_id);
    await using var reader = await cmd.ExecuteReaderAsync();
    if (!await reader.ReadAsync()) return null;
    return new GetOrderTotalRow(
        reader.IsDBNull(0) ? null : reader.GetDouble(0)
    );
}

public static async Task<int> DeleteOrdersByUser(SqliteConnection conn, int user_id) {
    await using var cmd = new SqliteCommand("DELETE FROM orders WHERE user_id = ?", conn);
    cmd.Parameters.AddWithValue("$p1", user_id);
    return await cmd.ExecuteNonQueryAsync();
}

public record GetUserByIdRow(
    int Id,
    string Name,
    string? Email,
    string Status,
    string CreatedAt
);

public static async Task<GetUserByIdRow?> GetUserById(SqliteConnection conn, int id) {
    await using var cmd = new SqliteCommand("SELECT id, name, email, status, created_at FROM users WHERE id = ?", conn);
    cmd.Parameters.AddWithValue("$p1", id);
    await using var reader = await cmd.ExecuteReaderAsync();
    if (!await reader.ReadAsync()) return null;
    return new GetUserByIdRow(
        reader.GetInt32(0),
        reader.GetString(1),
        reader.IsDBNull(2) ? null : reader.GetString(2),
        reader.GetString(3),
        reader.GetString(4)
    );
}

public record ListActiveUsersRow(
    int Id,
    string Name,
    string? Email
);

public static async Task<List<ListActiveUsersRow>> ListActiveUsers(SqliteConnection conn, string status) {
    await using var cmd = new SqliteCommand("SELECT id, name, email FROM users WHERE status = ?", conn);
    cmd.Parameters.AddWithValue("$p1", status);
    await using var reader = await cmd.ExecuteReaderAsync();
    var results = new List<ListActiveUsersRow>();
    while (await reader.ReadAsync()) {
        results.Add(new ListActiveUsersRow(
            reader.GetInt32(0),
            reader.GetString(1),
            reader.IsDBNull(2) ? null : reader.GetString(2)
        ));
    }
    return results;
}

public static async Task CreateUser(SqliteConnection conn, string name, string email, string status) {
    await using var cmd = new SqliteCommand("INSERT INTO users (name, email, status) VALUES (?, ?, ?)", conn);
    cmd.Parameters.AddWithValue("$p1", name);
    cmd.Parameters.AddWithValue("$p2", email);
    cmd.Parameters.AddWithValue("$p3", status);
    await cmd.ExecuteNonQueryAsync();
}

public static async Task UpdateUserEmail(SqliteConnection conn, string email, int id) {
    await using var cmd = new SqliteCommand("UPDATE users SET email = ? WHERE id = ?", conn);
    cmd.Parameters.AddWithValue("$p1", email);
    cmd.Parameters.AddWithValue("$p2", id);
    await cmd.ExecuteNonQueryAsync();
}

public static async Task DeleteUser(SqliteConnection conn, int id) {
    await using var cmd = new SqliteCommand("DELETE FROM users WHERE id = ?", conn);
    cmd.Parameters.AddWithValue("$p1", id);
    await cmd.ExecuteNonQueryAsync();
}

public record SearchUsersRow(
    int Id,
    string Name,
    string? Email
);

public static async Task<List<SearchUsersRow>> SearchUsers(SqliteConnection conn, string name) {
    await using var cmd = new SqliteCommand("SELECT id, name, email FROM users WHERE name LIKE ?", conn);
    cmd.Parameters.AddWithValue("$p1", name);
    await using var reader = await cmd.ExecuteReaderAsync();
    var results = new List<SearchUsersRow>();
    while (await reader.ReadAsync()) {
        results.Add(new SearchUsersRow(
            reader.GetInt32(0),
            reader.GetString(1),
            reader.IsDBNull(2) ? null : reader.GetString(2)
        ));
    }
    return results;
}

}
