<?php

declare(strict_types=1);

// Auto-generated by scythe. Do not edit.


enum UserStatus: string {
    case active = "active";
    case inactive = "inactive";
    case banned = "banned";
}

readonly class CreateOrderRow {
    public function __construct(
        public int $id,
        public int $user_id,
        public string $total,
        public ?string $notes,
        public \DateTimeImmutable $created_at,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            user_id: (int) $row['user_id'],
            total: (string) $row['total'],
            notes: $row['notes'] !== null ? (string) $row['notes'] : null,
            created_at: new \DateTimeImmutable($row['created_at']),
        );
    }
}

function createOrder(PDO $pdo, int $user_id, string $total, string $notes): ?CreateOrderRow {
    $stmt = $pdo->prepare("INSERT INTO orders (user_id, total, notes) VALUES (:p1, :p2, :p3) RETURNING id, user_id, total, notes, created_at");
    $stmt->execute(["p1" => $user_id, "p2" => $total, "p3" => $notes]);
    $row = $stmt->fetch(PDO::FETCH_ASSOC);
    return $row ? CreateOrderRow::fromRow($row) : null;
}

readonly class GetOrdersByUserRow {
    public function __construct(
        public int $id,
        public string $total,
        public ?string $notes,
        public \DateTimeImmutable $created_at,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            total: (string) $row['total'],
            notes: $row['notes'] !== null ? (string) $row['notes'] : null,
            created_at: new \DateTimeImmutable($row['created_at']),
        );
    }
}

function getOrdersByUser(PDO $pdo, int $user_id): array {
    $stmt = $pdo->prepare("SELECT id, total, notes, created_at FROM orders WHERE user_id = :p1 ORDER BY created_at DESC");
    $stmt->execute(["p1" => $user_id]);
    $rows = $stmt->fetchAll(PDO::FETCH_ASSOC);
    return array_map([GetOrdersByUserRow::class, 'fromRow'], $rows);
}

readonly class GetOrderTotalRow {
    public function __construct(
        public ?string $total_sum,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            total_sum: $row['total_sum'] !== null ? (string) $row['total_sum'] : null,
        );
    }
}

function getOrderTotal(PDO $pdo, int $user_id): ?GetOrderTotalRow {
    $stmt = $pdo->prepare("SELECT SUM(total) AS total_sum FROM orders WHERE user_id = :p1");
    $stmt->execute(["p1" => $user_id]);
    $row = $stmt->fetch(PDO::FETCH_ASSOC);
    return $row ? GetOrderTotalRow::fromRow($row) : null;
}

function deleteOrdersByUser(PDO $pdo, int $user_id): int {
    $stmt = $pdo->prepare("DELETE FROM orders WHERE user_id = :p1");
    $stmt->execute(["p1" => $user_id]);
    return $stmt->rowCount();
}

readonly class GetUserByIdRow {
    public function __construct(
        public int $id,
        public string $name,
        public ?string $email,
        public UserStatus $status,
        public \DateTimeImmutable $created_at,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            name: (string) $row['name'],
            email: $row['email'] !== null ? (string) $row['email'] : null,
            status: UserStatus::from($row['status']),
            created_at: new \DateTimeImmutable($row['created_at']),
        );
    }
}

function getUserById(PDO $pdo, int $id): ?GetUserByIdRow {
    $stmt = $pdo->prepare("SELECT id, name, email, status, created_at FROM users WHERE id = :p1");
    $stmt->execute(["p1" => $id]);
    $row = $stmt->fetch(PDO::FETCH_ASSOC);
    return $row ? GetUserByIdRow::fromRow($row) : null;
}

readonly class ListActiveUsersRow {
    public function __construct(
        public int $id,
        public string $name,
        public ?string $email,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            name: (string) $row['name'],
            email: $row['email'] !== null ? (string) $row['email'] : null,
        );
    }
}

function listActiveUsers(PDO $pdo, UserStatus $status): array {
    $stmt = $pdo->prepare("SELECT id, name, email FROM users WHERE status = :p1");
    $stmt->execute(["p1" => $status->value]);
    $rows = $stmt->fetchAll(PDO::FETCH_ASSOC);
    return array_map([ListActiveUsersRow::class, 'fromRow'], $rows);
}

readonly class CreateUserRow {
    public function __construct(
        public int $id,
        public string $name,
        public ?string $email,
        public UserStatus $status,
        public \DateTimeImmutable $created_at,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            name: (string) $row['name'],
            email: $row['email'] !== null ? (string) $row['email'] : null,
            status: UserStatus::from($row['status']),
            created_at: new \DateTimeImmutable($row['created_at']),
        );
    }
}

function createUser(PDO $pdo, string $name, string $email, UserStatus $status): ?CreateUserRow {
    $stmt = $pdo->prepare("INSERT INTO users (name, email, status) VALUES (:p1, :p2, :p3) RETURNING id, name, email, status, created_at");
    $stmt->execute(["p1" => $name, "p2" => $email, "p3" => $status->value]);
    $row = $stmt->fetch(PDO::FETCH_ASSOC);
    return $row ? CreateUserRow::fromRow($row) : null;
}

function updateUserEmail(PDO $pdo, string $email, int $id): void {
    $stmt = $pdo->prepare("UPDATE users SET email = :p1 WHERE id = :p2");
    $stmt->execute(["p1" => $email, "p2" => $id]);
}

function deleteUser(PDO $pdo, int $id): void {
    $stmt = $pdo->prepare("DELETE FROM users WHERE id = :p1");
    $stmt->execute(["p1" => $id]);
}

readonly class GetUserOrdersRow {
    public function __construct(
        public int $id,
        public string $name,
        public ?string $total,
        public ?string $notes,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            name: (string) $row['name'],
            total: $row['total'] !== null ? (string) $row['total'] : null,
            notes: $row['notes'] !== null ? (string) $row['notes'] : null,
        );
    }
}

function getUserOrders(PDO $pdo, UserStatus $status): array {
    $stmt = $pdo->prepare("SELECT u.id, u.name, o.total, o.notes FROM users u LEFT JOIN orders o ON u.id = o.user_id WHERE u.status = :p1");
    $stmt->execute(["p1" => $status->value]);
    $rows = $stmt->fetchAll(PDO::FETCH_ASSOC);
    return array_map([GetUserOrdersRow::class, 'fromRow'], $rows);
}

readonly class CountUsersByStatusRow {
    public function __construct(
        public UserStatus $status,
        public int $user_count,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            status: UserStatus::from($row['status']),
            user_count: (int) $row['user_count'],
        );
    }
}

function countUsersByStatus(PDO $pdo, UserStatus $status): ?CountUsersByStatusRow {
    $stmt = $pdo->prepare("SELECT status, COUNT(*) AS user_count FROM users GROUP BY status HAVING status = :p1");
    $stmt->execute(["p1" => $status->value]);
    $row = $stmt->fetch(PDO::FETCH_ASSOC);
    return $row ? CountUsersByStatusRow::fromRow($row) : null;
}

readonly class GetUserWithTagsRow {
    public function __construct(
        public int $id,
        public string $name,
        public string $tag_name,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            name: (string) $row['name'],
            tag_name: (string) $row['tag_name'],
        );
    }
}

function getUserWithTags(PDO $pdo, int $id): array {
    $stmt = $pdo->prepare("SELECT u.id, u.name, t.name AS tag_name FROM users u INNER JOIN user_tags ut ON u.id = ut.user_id INNER JOIN tags t ON ut.tag_id = t.id WHERE u.id = :p1");
    $stmt->execute(["p1" => $id]);
    $rows = $stmt->fetchAll(PDO::FETCH_ASSOC);
    return array_map([GetUserWithTagsRow::class, 'fromRow'], $rows);
}

readonly class SearchUsersRow {
    public function __construct(
        public int $id,
        public string $name,
        public ?string $email,
    ) {}

    public static function fromRow(array $row): self {
        return new self(
            id: (int) $row['id'],
            name: (string) $row['name'],
            email: $row['email'] !== null ? (string) $row['email'] : null,
        );
    }
}

function searchUsers(PDO $pdo, string $name): array {
    $stmt = $pdo->prepare("SELECT id, name, email FROM users WHERE name LIKE :p1");
    $stmt->execute(["p1" => $name]);
    $rows = $stmt->fetchAll(PDO::FETCH_ASSOC);
    return array_map([SearchUsersRow::class, 'fromRow'], $rows);
}
