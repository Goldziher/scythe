{
  "name": "error_duplicate_column_alias",
  "description": "Duplicate column aliases in SELECT should produce an error",
  "category": "errors/duplicate_column_alias",
  "tags": ["error", "alias", "duplicate"],
  "schema_sql": [
    "CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT NOT NULL);"
  ],
  "query_sql": "-- @name DuplicateAlias\n-- @returns :many\nSELECT id AS x, name AS x FROM users",
  "expected": {
    "success": false,
    "error": {
      "code": "DUPLICATE_ALIAS",
      "message_contains": "duplicate"
    }
  },
  "source": "original"
}
