{
  "name": "name_value_not_an_identifier",
  "description": "An @name value that cannot be an identifier in generated code is rejected",
  "category": "errors/invalid_annotation",
  "tags": ["error", "annotation", "name"],
  "schema_sql": ["CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL);"],
  "query_sql": "-- @name get-user\n-- @returns :many\nSELECT id, name FROM users;",
  "config": {
    "engine": "postgresql",
    "gen": {
      "target": "sqlx"
    }
  },
  "expected": {
    "success": false,
    "error": {
      "code": "INVALID_ANNOTATION",
      "message_contains": "only ASCII letters, digits and underscores"
    }
  },
  "source": "original"
}
