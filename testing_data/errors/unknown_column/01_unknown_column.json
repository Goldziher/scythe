{
  "name": "unknown_column",
  "description": "Reference a nonexistent column in SELECT, expecting an error",
  "category": "errors/unknown_column",
  "tags": ["error", "unknown_column"],
  "schema_sql": [
    "CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT NOT NULL);"
  ],
  "query_sql": "-- @name GetNonexistent\n-- @returns :many\nSELECT nonexistent FROM users;",
  "config": {
    "engine": "postgresql",
    "gen": {
      "target": "sqlx"
    }
  },
  "expected": {
    "success": false,
    "error": {
      "code": "UNKNOWN_COLUMN",
      "message_contains": "column \"nonexistent\" does not exist"
    }
  },
  "source": "sqlc"
}
