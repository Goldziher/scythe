{
  "category": "aggregates/numeric_widening",
  "description": "SUM(integer) widens to int64 (bigint), matching PostgreSQL's sum(integer) -> bigint",
  "expected": {
    "query": {
      "columns": [
        {
          "name": "med_sum",
          "nullable": true,
          "type": "int64"
        }
      ],
      "command": "one",
      "name": "SumMed",
      "params": []
    },
    "success": true
  },
  "name": "sum_int32_widens_to_int64",
  "query_sql": "-- @name SumMed\n-- @returns :one\nSELECT SUM(med) AS med_sum FROM numeric_data;",
  "schema_sql": [
    "CREATE TABLE numeric_data (id SERIAL PRIMARY KEY, small SMALLINT NOT NULL, med INTEGER NOT NULL, big BIGINT NOT NULL, r REAL NOT NULL, d DOUBLE PRECISION NOT NULL, n NUMERIC(10,2) NOT NULL);"
  ],
  "source": "original",
  "tags": ["aggregate", "sum", "numeric_widening", "int32", "int64"]
}
