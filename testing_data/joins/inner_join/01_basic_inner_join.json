{
  "category": "joins/inner_join",
  "description": "INNER JOIN between users and orders, all selected columns remain non-nullable",
  "expected": {
    "query": {
      "columns": [
        {
          "name": "id",
          "nullable": false,
          "type": "int32"
        },
        {
          "name": "name",
          "nullable": false,
          "type": "string"
        },
        {
          "name": "total",
          "nullable": false,
          "type": "int32"
        }
      ],
      "command": "many",
      "name": "GetUserOrders",
      "params": []
    },
    "success": true
  },
  "name": "basic_inner_join",
  "query_sql": "-- @name GetUserOrders\n-- @returns :many\nSELECT u.id, u.name, o.total FROM users u INNER JOIN orders o ON u.id = o.user_id",
  "schema_sql": [
    "CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active', bio TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now())",
    "CREATE TABLE orders (id SERIAL PRIMARY KEY, user_id INTEGER NOT NULL REFERENCES users(id), total INTEGER NOT NULL, notes TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now())"
  ],
  "source": "sqlc",
  "tags": [
    "join",
    "inner_join"
  ]
}
