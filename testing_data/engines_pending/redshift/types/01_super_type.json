{
	"category": "engines/redshift/types",
	"config": { "engine": "redshift", "gen": { "target": "sqlx" } },
	"description": "Redshift SUPER type maps to string/json",
	"expected": {
		"query": {
			"name": "GetEvents",
			"command": "many",
			"columns": [
				{ "name": "id", "nullable": false, "type": "int32" },
				{ "name": "data", "nullable": true, "type": "json" }
			],
			"params": []
		},
		"success": true
	},
	"name": "redshift_super_type",
	"query_sql": "-- @name GetEvents\n-- @returns :many\nSELECT id, data FROM events;",
	"schema_sql": ["CREATE TABLE events (id INTEGER NOT NULL, data SUPER);"],
	"source": "original",
	"tags": ["redshift", "super"]
}
