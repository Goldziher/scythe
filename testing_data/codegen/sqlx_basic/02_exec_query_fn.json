{
  "category": "codegen/sqlx_basic",
  "config": {
    "engine": "postgresql",
    "gen": {
      "target": "sqlx"
    }
  },
  "description": "Codegen test for :exec query showing no row struct, only execute function",
  "expected": {
    "generated_code": {
      "rust-sqlx": {
        "query_fn": "pub async fn purge_users(pool: &sqlx::PgPool, id: i32) -> Result<(), sqlx::Error> {\n    sqlx::query!(\"DELETE FROM users WHERE id < $1\", id)\n        .execute(pool)\n        .await?;\n    Ok(())\n}"
      }
    },
    "query": {
      "columns": [],
      "command": "exec",
      "name": "PurgeUsers",
      "params": [
        {
          "name": "id",
          "nullable": false,
          "type": "int32"
        }
      ]
    },
    "success": true
  },
  "name": "exec_query_fn",
  "query_sql": "-- @name PurgeUsers\n-- @returns :exec\nDELETE FROM users WHERE id < $1;",
  "schema_sql": [
    "CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT NOT NULL);"
  ],
  "source": "sqlc",
  "tags": [
    "codegen",
    "sqlx",
    "exec"
  ]
}
