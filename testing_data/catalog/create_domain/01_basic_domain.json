{
	"name": "basic_domain",
	"description": "CREATE DOMAIN with CHECK constraint for positive integers",
	"category": "catalog/create_domain",
	"tags": ["domain", "check"],
	"schema_sql": ["CREATE DOMAIN positive_int AS integer CHECK (VALUE > 0)"],
	"expected": {
		"success": true,
		"catalog": {
			"tables": {},
			"enums": {},
			"composites": {}
		}
	},
	"source": "original"
}
