{
	"category": "params/complex_positions",
	"description": "Parameters used in LIMIT and OFFSET clauses for pagination",
	"expected": {
		"query": {
			"columns": [
				{
					"name": "id",
					"nullable": false,
					"type": "int32"
				},
				{
					"name": "name",
					"nullable": false,
					"type": "string"
				}
			],
			"command": "many",
			"name": "ListUsersPaginated",
			"params": [
				{
					"name": "limit_val",
					"nullable": false,
					"position": 1,
					"type": "int64"
				},
				{
					"name": "offset_val",
					"nullable": false,
					"position": 2,
					"type": "int64"
				}
			]
		},
		"success": true
	},
	"name": "param_as_limit_offset",
	"query_sql": "-- @name ListUsersPaginated\n-- @returns :many\nSELECT id, name FROM users ORDER BY id LIMIT $1 OFFSET $2",
	"schema_sql": [
		"CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT NOT NULL);"
	],
	"source": "original",
	"tags": ["param", "limit", "offset", "pagination"]
}
