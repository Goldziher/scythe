{
  "category": "params/explicit_cast",
  "config": {
    "engine": "postgresql",
    "gen": {
      "target": "sqlx"
    }
  },
  "description": "Explicit cast $1::uuid determines parameter type as uuid::Uuid",
  "expected": {
    "generated_code": {
      "rust-sqlx": {
        "row_struct": "#[derive(Debug, sqlx::FromRow)]\npub struct GetUserByCastUuidRow {\n    pub id: uuid::Uuid,\n    pub name: String,\n}"
      }
    },
    "query": {
      "columns": [
        {
          "name": "id",
          "nullable": false,
          "type": "uuid"
        },
        {
          "name": "name",
          "nullable": false,
          "type": "string"
        }
      ],
      "command": "one",
      "name": "GetUserByCastUuid",
      "params": [
        {
          "name": "id",
          "nullable": false,
          "type": "uuid"
        }
      ]
    },
    "success": true
  },
  "name": "cast_to_uuid",
  "query_sql": "-- @name GetUserByCastUuid\n-- @returns :one\nSELECT id, name FROM users WHERE id = $1::uuid;",
  "schema_sql": [
    "CREATE TABLE users (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), name TEXT NOT NULL);"
  ],
  "source": "sqlc_expanded",
  "tags": [
    "param",
    "cast",
    "uuid"
  ]
}
