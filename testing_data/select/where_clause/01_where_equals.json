{
  "category": "select/where_clause",
  "description": "WHERE clause with equality comparison and string parameter",
  "expected": {
    "query": {
      "columns": [
        {
          "name": "id",
          "nullable": false,
          "type": "int32"
        },
        {
          "name": "name",
          "nullable": false,
          "type": "string"
        },
        {
          "name": "email",
          "nullable": false,
          "type": "string"
        }
      ],
      "command": "many",
      "name": "GetUserByName",
      "params": [
        {
          "name": "name",
          "nullable": false,
          "position": 1,
          "type": "string"
        }
      ]
    },
    "success": true
  },
  "name": "where_equals",
  "query_sql": "-- @name GetUserByName\n-- @returns :many\nSELECT id, name, email FROM users WHERE name = $1",
  "schema_sql": [
    "CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active', bio TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now())"
  ],
  "source": "sqlc",
  "tags": [
    "select",
    "where",
    "equals",
    "param"
  ]
}
