{
	"category": "expressions/math_functions",
	"description": "ROUND with cast to numeric returns rust_decimal::Decimal non-nullable",
	"expected": {
		"query": {
			"columns": [
				{
					"name": "id",
					"nullable": false,
					"type": "int32"
				},
				{
					"name": "rounded",
					"nullable": false,
					"type": "decimal"
				}
			],
			"command": "many",
			"name": "RoundValue",
			"params": []
		},
		"success": true
	},
	"name": "math_round",
	"query_sql": "-- @name RoundValue\n-- @returns :many\nSELECT id, ROUND(value::numeric, 2) AS rounded FROM measurements",
	"schema_sql": [
		"CREATE TABLE measurements (id SERIAL PRIMARY KEY, value DOUBLE PRECISION NOT NULL, quantity INTEGER NOT NULL);"
	],
	"source": "original",
	"tags": ["expression", "math", "round", "numeric"]
}
