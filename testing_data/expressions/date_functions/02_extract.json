{
  "category": "expressions/date_functions",
  "description": "EXTRACT returns numeric (decimal) on PostgreSQL 14+, not the double precision every version of date_part() returns",
  "expected": {
    "query": {
      "columns": [
        {
          "name": "id",
          "nullable": false,
          "type": "int32"
        },
        {
          "name": "yr",
          "note": "PostgreSQL 14+ EXTRACT returns numeric; see #123",
          "nullable": false,
          "type": "decimal"
        },
        {
          "name": "mo",
          "note": "PostgreSQL 14+ EXTRACT returns numeric; see #123",
          "nullable": false,
          "type": "decimal"
        }
      ],
      "command": "many",
      "name": "ExtractDateParts",
      "params": []
    },
    "success": true
  },
  "name": "date_extract",
  "query_sql": "-- @name ExtractDateParts\n-- @returns :many\nSELECT id, EXTRACT(YEAR FROM occurred_at) AS yr, EXTRACT(MONTH FROM occurred_at) AS mo FROM events",
  "schema_sql": ["CREATE TABLE events (id SERIAL PRIMARY KEY, name TEXT NOT NULL, occurred_at TIMESTAMPTZ NOT NULL);"],
  "source": "original",
  "tags": ["expression", "date", "extract"]
}
