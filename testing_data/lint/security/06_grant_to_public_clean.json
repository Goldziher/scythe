{
	"name": "grant_to_public_clean",
	"description": "GRANT to a specific role should not trigger SC-SEC03",
	"category": "lint/security",
	"tags": ["lint", "security", "grant"],
	"schema_sql": ["CREATE TABLE users (id SERIAL PRIMARY KEY);"],
	"query_sql": "-- @name CreateRoleGrant\n-- @returns :exec\nGRANT SELECT ON users TO app_role;",
	"expected": {
		"success": true,
		"lint": {
			"violations": []
		}
	},
	"source": "original"
}
