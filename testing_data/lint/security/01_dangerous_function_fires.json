{
	"name": "dangerous_function_fires",
	"description": "Calling pg_read_file should trigger SC-SEC01",
	"category": "lint/security",
	"tags": ["lint", "security", "dangerous-function"],
	"schema_sql": [
		"CREATE TABLE files (id SERIAL PRIMARY KEY, path TEXT NOT NULL);"
	],
	"query_sql": "-- @name GetServerFileSize\n-- @returns :one\nSELECT length(pg_read_file('/etc/passwd')) AS sz;",
	"expected": {
		"success": true,
		"lint": {
			"violations": [
				{
					"rule_code": "SC-SEC01",
					"message_contains": "dangerous function"
				}
			]
		}
	},
	"source": "original"
}
