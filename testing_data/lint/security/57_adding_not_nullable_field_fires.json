{
	"name": "adding_not_nullable_field_fires",
	"description": "ALTER TABLE ADD COLUMN NOT NULL without DEFAULT should trigger SC-MIG18",
	"category": "lint/security",
	"tags": ["lint", "migration", "adding-not-nullable-field"],
	"schema_sql": ["CREATE TABLE users (id bigint PRIMARY KEY)"],
	"query_sql": "-- @name UpdateUsersAddRequiredEmail\n-- @returns :exec\nALTER TABLE users ADD COLUMN email text NOT NULL",
	"expected": {
		"success": true,
		"lint": {
			"violations": [
				{
					"rule_code": "SC-MIG18",
					"message_contains": "DEFAULT"
				}
			]
		}
	},
	"source": "original"
}
