{
	"name": "check_constraint_always_true_clean",
	"description": "CREATE TABLE with a real CHECK predicate should not trigger SC-CHK01",
	"category": "lint/security",
	"tags": ["lint", "antipattern", "check-constraint-always-true"],
	"schema_sql": [],
	"query_sql": "-- @name CreateAccountsTable\n-- @returns :exec\nCREATE TABLE accounts (id bigint PRIMARY KEY, balance numeric CHECK (balance >= 0))",
	"expected": {
		"success": true,
		"lint": {
			"violations": []
		}
	},
	"source": "original"
}
