{
	"name": "ambiguous_column_fires",
	"description": "Unqualified column in JOIN that exists in multiple tables should trigger SC-S06",
	"category": "lint/safety",
	"tags": ["lint", "safety", "ambiguous", "join"],
	"schema_sql": [
		"CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, status TEXT);",
		"CREATE TABLE orders (id SERIAL PRIMARY KEY, user_id INT NOT NULL, total INT NOT NULL);"
	],
	"query_sql": "-- @name GetUserOrders\n-- @returns :many\nSELECT id FROM users u JOIN orders o ON u.id = o.user_id;",
	"expected": {
		"success": true,
		"lint": {
			"violations": [
				{
					"rule_code": "SC-S06",
					"message_contains": "ambiguous"
				}
			]
		}
	},
	"source": "original"
}
