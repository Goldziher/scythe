{
	"name": "non_snake_case_alias_fires",
	"description": "Column alias in camelCase should trigger SC-N01",
	"category": "lint/naming",
	"tags": ["lint", "naming", "alias", "snake_case"],
	"schema_sql": [
		"CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, status TEXT);"
	],
	"query_sql": "-- @name GetUsers\n-- @returns :many\nSELECT id AS userId FROM users;",
	"expected": {
		"success": true,
		"lint": {
			"violations": [
				{
					"rule_code": "SC-N01",
					"message_contains": "snake_case"
				}
			]
		}
	},
	"source": "original"
}
