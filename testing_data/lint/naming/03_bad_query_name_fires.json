{
	"name": "bad_query_name_fires",
	"description": "Query name in camelCase (not PascalCase) should trigger SC-N03",
	"category": "lint/naming",
	"tags": ["lint", "naming", "query_name", "pascal_case"],
	"schema_sql": [
		"CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, status TEXT);"
	],
	"query_sql": "-- @name doSomething\n-- @returns :many\nSELECT id, name FROM users;",
	"expected": {
		"success": true,
		"lint": {
			"violations": [
				{
					"rule_code": "SC-N03",
					"message_contains": "PascalCase"
				}
			]
		}
	},
	"source": "original"
}
