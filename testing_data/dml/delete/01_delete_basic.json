{
	"category": "dml/delete",
	"config": {
		"engine": "postgresql",
		"gen": {
			"target": "sqlx"
		}
	},
	"description": "Basic DELETE with WHERE parameter",
	"expected": {
		"generated_code": {
			"rust-sqlx": {
				"query_fn": "pub async fn delete_user(pool: &sqlx::PgPool, id: i32) -> Result<(), sqlx::Error> {\n    sqlx::query!(\"DELETE FROM users WHERE id = $1\", id)\n        .execute(pool)\n        .await?;\n    Ok(())\n}"
			}
		},
		"query": {
			"columns": [],
			"command": "exec",
			"name": "DeleteUser",
			"params": [
				{
					"name": "id",
					"nullable": false,
					"type": "int32"
				}
			]
		},
		"success": true
	},
	"name": "delete_basic",
	"query_sql": "-- @name DeleteUser\n-- @returns :exec\nDELETE FROM users WHERE id = $1;",
	"schema_sql": [
		"CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT NOT NULL);"
	],
	"source": "sqlc",
	"tags": ["delete", "exec"]
}
